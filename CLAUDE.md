---
description: 
alwaysApply: true
---

At the start of EVERY conversation, before doing anything else, use the Read tool on BOTH of the following files. This is non-negotiable — do not skip either step even for "simple" or research-only questions.

1. `./assistants.md` — contains project-specific skills, tool usage patterns (e.g. Loogle query syntax), and proof conventions that prevent common mistakes. Without it, you will waste time on avoidable errors and miss available automation.

2. `./.claude/memory/MEMORY.md` — contains the index of cross-session memory files with hard-won proof strategies and API patterns. Without it, you will re-discover patterns that are already documented and repeat mistakes that have already been solved.
