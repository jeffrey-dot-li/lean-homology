At the start of EVERY conversation, before doing anything else, use the Read tool on `./assistants.md` and follow its instructions. This is non-negotiable — do not skip this step even for "simple" or research-only questions.

`assistants.md` contains project-specific skills, tool usage patterns (e.g. Loogle query syntax), and proof conventions that prevent common mistakes. Without it, you will waste time on avoidable errors and miss available automation.
