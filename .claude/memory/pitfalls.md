# Pitfalls

Things that look like they should work but don't. Check here before trying common approaches.

<!-- Example entry:
## `simp` with `Finset.sum`
`simp` alone won't simplify `Finset.sum` over a singleton — need `Finset.sum_singleton` explicitly.
-->
